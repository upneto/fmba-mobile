<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.com.fiap.fmba">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FMBAMOBILE"
        tools:targetApi="31">
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.ManutencaoOrdemServicoActivity"
            android:exported="false"
            android:parentActivityName=".ui.activity.ListaOrdemServicoActivity" />
        <activity
            android:name=".ui.activity.CadastroOrdemServicoActivity"
            android:exported="false"
            android:parentActivityName=".ui.activity.ListaOrdemServicoActivity" />
        <activity
            android:name=".ui.activity.ListaOrdemServicoActivity"
            android:exported="false"
            android:launchMode="singleTop" />
        <activity
            android:name=".ui.activity.DetalheOrdemServicoActivity"
            android:exported="false"
            android:parentActivityName=".ui.activity.ListaOrdemServicoActivity" />
    </application>

</manifest>